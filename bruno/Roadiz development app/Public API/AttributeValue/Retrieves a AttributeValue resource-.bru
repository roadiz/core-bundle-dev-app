meta {
  name: Retrieves a AttributeValue resource-
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/attribute_values/:id
  body: none
  auth: bearer
}

params:query {
  ~_preview: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
