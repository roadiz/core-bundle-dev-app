framework:
    notifier:
        chatter_transports:
        texter_transports:
        channel_policy:
            urgent: ['email']
            high: ['email']
            medium: ['email']
            low: ['email']
        admin_recipients:
            - { email: '%env(DEFAULT_ADMIN_NOTIFIER_RECIPIENT)%' }
