{% embed "@RoadizRozier/macros/rz_actions_menu.html.twig" with {
    vertical: true,
    class: 'actions-menu',
    setting: setting,
} only %}
    {% block items %}
        {% if setting.getId %}
            {{ _self.deleteItem(href: path('settingsDeletePage', { settingId: setting.getId})) }}
        {% endif %}
    {% endblock %}
{% endembed %}
