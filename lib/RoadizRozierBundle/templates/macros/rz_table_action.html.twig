
{% macro item(options = {}) %}
	{% import '@RoadizRozier/macros/rz_button.html.twig' as rz_button %}

	{% set default_options = {
		size: 'xs',
		emphasis: 'primary',
	} %}

	{{ rz_button.root(options|merge(default_options)) }}
{% endmacro %}
