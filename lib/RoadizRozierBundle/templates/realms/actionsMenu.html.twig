{% embed "@RoadizRozier/macros/rz_actions_menu.html.twig" with {
    vertical: true,
    class: 'actions-menu',
    item: item
} only %}
    {% block items %}
        {% if item.id %}
            {{ _self.deleteItem(href: path('realmsDeletePage', { id: item.id })) }}
        {% endif %}
    {% endblock %}
{% endembed %}
