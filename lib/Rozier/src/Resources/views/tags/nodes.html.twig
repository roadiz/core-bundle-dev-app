{% extends '@RoadizRozier/layout.html.twig' %}
{% set tTag = tag.getTranslatedTags.first %}

{%- block title -%}
    {{ tTag.getName }}
    |
    {{ parent() -}}
{%- endblock -%}

{% block content %}
<section class="content-global content-nav-and-filters">
    <header class="content-header header-tags header-tags-nodes">
        <h1 class="content-title tags-nodes-title">{{ tTag.getName }}</h1>
        {% include '@RoadizRozier/widgets/countFiltersBar.html.twig' %}
        {% include '@RoadizRozier/tags/navBack.html.twig' with {} only %}
        {% include '@RoadizRozier/tags/navBar.html.twig' with {"tag": tag, "current": 'nodes'} only %}
    </header>

    {% include '@RoadizRozier/widgets/filtersBar.html.twig' %}

    <article class="content content-no-action-menu content-tags-nodes">
        <div class="content-table-cont">
            <table class="tags content-table uk-table">
                <thead>
                {% include '@RoadizRozier/nodes/widgets/node-header.html.twig' with {
                    'filters': filters
                } only %}
                </thead>
                <tbody>
                    {% for node in nodes %}
                        {% include '@RoadizRozier/nodes/widgets/node-row.html.twig' with {
                            'node': node
                        } only %}
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </article>

</section>
{% endblock %}
