{%
set listItems = [
    [
        {
            'name': 'edit',
            'label': 'folder.settings',
            'href': path('foldersEditPage', { folderId: folder.id }),
        },
        {
            'name': 'edit-label',
            'label': 'folder.labels',
            'href': path('foldersEditTranslationPage', { folderId: folder.id, translationId: translation.id }),
        },
        {
            'name': 'list',
            'label': 'documents',
            'href': path('documentsHomePage', { folderId: folder.id }),
        },
        {
            'name': 'private_list',
            'label': 'private_documents',
            'href': path('documentsPrivateHomePage', { folderId: folder.id }),
        },
    ]
]
%}
{% set custom_entries_content %}
    {% block additional_entries %}{% endblock %}
{% endset %}

{% embed "@RoadizRozier/macros/rz_menu_bar.html.twig" with {listItems : listItems, current: current} %}
    {% block additional_entries %}
        {{ custom_entries_content|raw }}
    {% endblock %}
{% endembed %}
