{% embed "@RoadizRozier/macros/rz_actions_menu.html.twig" with {
    vertical: true,
    class: 'actions-menu',
    user: user
} only %}
    {% block items %}
        {% if is_granted('ROLE_ACCESS_USERS_DELETE') and user.id %}
            {{ _self.deleteItem(href: path('usersDeletePage', { id: user.id})) }}
        {% endif %}
    {% endblock %}
{% endembed %}
