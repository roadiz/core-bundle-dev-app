{% embed "@RoadizRozier/macros/rz_floating_bar.html.twig" with {
    vertical: true,
    class: 'actions-menu'
} %}
    {% block items %}
        {% if is_granted('ROLE_ACCESS_NODETYPES') and item.id %}
            {{ _self.item(
                label: 'delete',
                class: 'rz-button--error',
                href: path('nodeTypeDecoratorsDeletePage', { id: item.id}),
                icon: 'rz-icon-ri--delete-bin-7-line'
            ) }}
        {% endif %}
    {% endblock %}
{% endembed %}
