{% macro item(label, class, href, icon, attr) %}
    {% if href is defined and href %}
        <a aria-label="{{ label|trans }}"
           href="{{ href }}"
           class="rz-button rz-button--md rz-button--on-dark {{ class is defined and class ? class : '' }}">
            <span class="rz-button__icon {{ icon }}"></span>
        </a>
    {% else %}
        <button aria-label="{{ label|trans }}"
                {% for attrname, attrvalue in attr %}
                    {{ attrname }}="{{ attrvalue }}"
                {% endfor %}
                class="rz-button rz-button--md rz-button--on-dark {{ class is defined and class ? class : '' }}">
            <span class="rz-button__icon {{ icon }}"></span>
        </button>
    {% endif %}
{% endmacro %}

{% macro saveItem(dataActionSave) %}
    {{ _self.item(
        label: 'save',
        class: 'rz-button--success',
        icon: 'rz-icon-ri--save-line',
        attr: {
            'is': 'rz-save-button',
            'data-action-save': dataActionSave,
            'type': 'submit'
        }
    ) }}
{% endmacro %}

<div class="rz-floating-bar{{ vertical ? ' rz-floating-bar--vertical' : '' }}{{ class is defined and class ? ' ' ~ class : '' }}">
    {% block items %}
    {% endblock %}
</div>
