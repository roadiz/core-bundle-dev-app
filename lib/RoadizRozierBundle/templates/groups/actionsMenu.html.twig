{% embed "@RoadizRozier/macros/rz_floating_bar.html.twig" with {
    vertical: true,
    class: 'actions-menu'
} %}
    {% block items %}
        {% if is_granted('ROLE_ACCESS_GROUPS') and is_granted(item) and item.id %}
            {{ _self.item(
                label: 'delete',
                class: 'rz-button--error',
                href: path('groupsDeletePage', { id: item.getId}),
                icon: 'rz-icon-ri--delete-bin-7-line'
            ) }}
        {% endif %}
    {% endblock %}
{% endembed %}
