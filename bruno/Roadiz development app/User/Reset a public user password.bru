meta {
  name: Reset a public user password
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/users/password_reset
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200) {
  }
}
