{% extends '@RoadizRozier/layout.html.twig' %}

{%- block title -%}
    {% trans %}add.a.settingGroup{% endtrans %}
    |
    {{ parent() -}}
{%- endblock -%}

{% block content %}
<section class="content-global">

    {% include '@RoadizRozier/settingGroups/head.html.twig' with {
        title: 'add.a.settingGroup'|trans,
        with_breadcrumb: true,
    } only %}

    <article class="content content-settingGroup-add">
        {% form_theme form '@RoadizRozier/forms.html.twig' %}
        {{ form_start(form, { attr: { id: 'add-settingGroup-form'}}) }}
            {{ form_widget(form) }}
            {% import "@RoadizRozier/macros/rz_actions_menu.html.twig" as actions_menu %}
            {{ actions_menu.saveItem('#add-settingGroup-form') }}
        {{ form_end(form) }}
    </article>

    {% include '@RoadizRozier/settingGroups/actionsMenu.html.twig' %}
</section>
{% endblock %}
