meta {
  name: Reset a public user password
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/users/password_reset
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200) {
  }
}
