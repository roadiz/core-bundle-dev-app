{% block content %}

{% include '@RoadizRozier/admin/confirm_action.html.twig' with {
    form: form,
    title: "reset.attribute.%name%.for_node.%nodeName%"|trans({
                '%name%': item|attribute_label(translation),
                '%nodeName%': node.nodeName
            }),
    messageType: 'warning',
    alertMessage: 'are_you_sure.reset.attribute_value'|trans,
    cancelPath: path('nodesEditAttributesPage', {
                    nodeId: node.id,
                    translationId: translation.id
                }),
    action_label: 'attributes.reset_value'|trans,
    action_icon: 'rz-icon-ri--refresh-line',
    action_color: 'color-default',
    headPath: '@RoadizRozier/nodes/head.html.twig',
} %}

{% endblock %}
