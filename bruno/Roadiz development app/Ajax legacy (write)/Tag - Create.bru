meta {
  name: Tag - Create
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/rz-admin/ajax/tag/create
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  csrfToken: {{ajaxToken}}
  tagName: My new tag
}

docs {
  `tagName` length must be between 3 and 200 characters.
}
